apiVersion: apps/v1
kind: Deployment
metadata:
  name: ncareer
  namespace: ncareer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ncareer
  template:
    metadata:
      labels:
        app: ncareer
    spec:
      containers:
        - name: ncareer
          image: leosunghojung/ncareer:f2ec06e
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
