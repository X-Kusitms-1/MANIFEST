# ilhaeng-backend-staging.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ilhaeng-backend-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/X-Kusitms-1/MANIFEST.git
    targetRevision: develop           # develop 브랜치로 바꿔도 됨
    path: staging                  # <- 이 디렉터리만 동기화
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true